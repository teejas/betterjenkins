apiVersion: apps/v1
kind: Deployment
metadata:
  name: betterjenkins-server
  labels:
    app: betterjenkins
spec:
  replicas: 1
  selector:
    matchLabels:
      app: betterjenkins
  template:
    metadata:
      labels:
        app: betterjenkins
    spec:
      containers:
      - name: betterjenkins-server
        image: tj1997/betterjenkins:server
        ports:
        - containerPort: 8080
        env:
        - name: DB_NAME
          value: betterjenkins
        - name: DB_HOST
          value: betterjenkins-db
        - name: DB_USER
          value: postgres
        - name: DB_PASSWORD
          value: postgres
---
apiVersion: v1
kind: Service
metadata:
  name: betterjenkins-server
  labels:
    app: betterjenkins
spec:
  selector:
    app: betterjenkins
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080